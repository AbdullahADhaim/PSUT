#include <string>
class Math {
private:
	
public: 
	static const float PI;
	static float areaOfTriangle(float l, float w);
	static long factorial(int n);
	static int fabs(int n);
	static int round(float  n);
};